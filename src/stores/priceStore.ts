import { atom} from 'nanostores';


const $fabricPrice = atom(0);
const $trimsAndAccessoriesPrice = atom(0);
const $progressPrice = atom(0);
const $washingProcessPrice = atom(0)
const $finishingPackingPrice = atom(0)

export {
    $fabricPrice,
    $trimsAndAccessoriesPrice,
    $progressPrice,
    $washingProcessPrice,
    $finishingPackingPrice
}